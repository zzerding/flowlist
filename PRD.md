# Flowlist 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 产品名称 | Flowlist |
| 文档版本 | v1.1 |
| 文档状态 | MVP 需求基线 |
| 更新日期 | 2026-09-09 |
| 输入依据 | [`README.MD`](README.MD)、外部资料调研 |

## 1. 产品概述

Flowlist 是一个受 Workflowy 启发的本地优先大纲工具。用户可在同一份无限层级大纲中记录笔记、拆解任务和整理想法，并在桌面、平板和手机上使用。应用支持安装为 PWA，首次成功加载后可离线工作。

MVP 的核心承诺是：不注册账号也能快速开始；编辑操作即时响应并自动保存在当前浏览器；层级、聚焦、搜索和排序共同构成完整的个人整理闭环。

## 2. 背景与问题

线性笔记不适合持续拆解主题与任务，传统文件夹又会增加分类和切换成本。Flowlist 用一棵可无限嵌套的节点树代替文件与文件夹，让用户在同一界面完成：

- 快速记录内容；
- 通过缩进表达父子关系；
- 聚焦一个分支以减少视觉干扰；
- 搜索并直接处理已有内容；
- 将任意节点作为待办跟踪；
- 在无网络环境继续使用。

## 3. 产品目标

### 3.1 MVP 目标

1. 用户无需账号即可创建、编辑和整理一份无限层级大纲。
2. 键盘用户可以连续完成新增、缩进、减缩进和文本编辑。
3. 用户可以通过聚焦、面包屑、搜索和折叠快速定位内容。
4. 节点内容、层级、状态和顺序可自动保存，并在刷新或重启浏览器后恢复。
5. 已加载过的应用在断网后仍可打开、读取、编辑和保存本地大纲。
6. 同一份代码适配桌面、平板和手机，并支持明暗主题。

### 3.2 非目标

MVP 不包含：

- 账号、登录和用户体系；
- 云端备份、跨设备同步和多人协作；
- 评论、权限、分享链接和版本历史；
- 多文档、文件夹和高级查询语法；
- 跨标签页实时协同或自动冲突合并；
- 第三方集成；
- 图片、附件、文本颜色/高亮、看板、AI、提醒、日历视图和自然语言日期解析；
- 对所有浏览器提供一致的主动安装提示。

## 4. 目标用户与核心场景

### 4.1 目标用户

- 希望用层级结构整理想法、笔记或计划的个人用户；
- 偏好键盘操作、需要快速捕捉内容的知识工作者；
- 需要在弱网或离线环境中查看和编辑个人清单的用户。

### 4.2 核心场景

1. **快速记录**：打开应用后直接输入，按 `Enter` 连续新增条目。
2. **拆解主题**：用 `Tab` 和 `Shift + Tab` 调整层级，把一个主题拆成子项。
3. **专注工作**：点击圆点聚焦某一分支，通过面包屑返回任意上级。
4. **查找处理**：搜索关键词，在结果上下文中编辑、完成或聚焦节点。
5. **调整顺序**：拖拽节点及其子树，重新安排同级顺序或父子关系。
6. **离线继续**：断网后重新打开已加载过的应用，继续编辑并保存到本地。

## 5. 产品原则与默认假设

以下是 README 未展开、为形成可执行 MVP 而采用的最小产品假设：

1. 应用只有一份本地大纲；不可见的根节点承载所有顶层节点。
2. 搜索范围是当前聚焦分支；位于根视图时即搜索整份大纲。
3. 任意节点可在普通条目和待办条目之间切换，完成状态不自动影响父子节点。
4. 拖动节点时连同其完整子树一起移动，且禁止移动到自己的后代之下。
5. “本地优先”仅表示当前浏览器 origin 下的本地保存，不代表备份或同步。
6. PWA 的“可安装”和“离线可用”是两个独立能力，分别验收。
7. 节点支持富文本标题和备注；MVP 不提供节点级多选（可通过聚焦祖先节点实现等效批量操作）。
8. 标签和日期是从原始文本即时识别的搜索标记，不维护独立实体。
9. 普通条目和段落在数据层完全同构，仅渲染不同：普通条目始终显示圆点，段落无圆点；两者均可包含子节点。
10. 分隔线与其他节点数据同构：可有子节点、可折叠、可聚焦；代码块节点的标题为纯文本，行内格式在其内不生效。
11. 删除不使用二次确认弹窗：删除立即生效并以可见撤销入口保护，撤销窗口内可完全恢复。
12. 折叠是纯展示状态，不限制搜索可及性：搜索可命中折叠子树，搜索期间的临时展开在清除搜索后按原折叠状态还原。

## 6. 功能需求

优先级定义：`P0` 为 MVP 发布必需；`P1` 为后续候选，本版本不实现。

### 6.1 大纲编辑

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-01 | P0 | 展示单份无限层级大纲 | 节点可包含任意层级子节点；展开后父子关系可辨识 |
| FR-02 | P0 | 编辑节点内容 | 点击或通过键盘聚焦节点后可编辑富文本标题；按 `Shift + Enter` 编辑备注；失焦、刷新后内容保持 |
| FR-03 | P0 | `Enter` 新建节点 | 在当前节点后创建同级节点，并将输入焦点移入新节点 |
| FR-04 | P0 | `Tab` 缩进 | 当前节点有前一个同级节点时，移动为其最后一个子节点；没有前一个同级节点时不改变结构 |
| FR-05 | P0 | `Shift + Tab` 减少缩进 | 非顶层节点移动到父节点之后并成为其同级；顶层节点不改变结构 |
| FR-06 | P0 | 删除空节点 | 节点文本为空且无子节点时，按 `Backspace` 删除并把焦点移到相邻节点；列表末尾渲染非持久化的空白输入位（UI 幻影行），幻影行输入即创建真实节点 |
| FR-07 | P0 | 删除节点和子树 | 节点菜单可删除当前节点；删除立即生效，显示带撤销按钮的 toast，撤销窗口内可完全恢复；刷新后已删除内容不可恢复 |

### 6.2 聚焦与导航

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-08 | P0 | 聚焦分支 | 点击节点圆点后，仅展示该节点及其后代；操作不改变树结构 |
| FR-09 | P0 | 面包屑导航 | 聚焦后展示从根到当前节点的路径；点击任意面包屑进入对应祖先层级 |
| FR-10 | P0 | 返回根视图 | 用户可通过面包屑一步返回整份大纲；原有文本、顺序、完成和折叠状态保持 |

### 6.3 搜索与折叠

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-11 | P0 | 搜索当前分支 | 搜索标题、备注、标签和日期文本；输入停止 120ms 后更新，匹配不区分大小写 |
| FR-12 | P0 | 保留搜索上下文 | 搜索可命中折叠子树内的节点，匹配节点及必要的祖先路径在结果中临时展开；无结果时显示明确空状态 |
| FR-13 | P0 | 搜索中操作 | 搜索结果引用原节点；编辑、完成或聚焦后修改反映到原树，不生成副本 |
| FR-14 | P0 | 清除搜索 | 清空搜索词后恢复搜索前的聚焦范围和折叠状态 |
| FR-15 | P0 | 折叠子树 | 有子节点的节点提供独立展开/折叠控件；折叠只影响显示，不删除内容 |

### 6.4 待办与排序

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-16 | P0 | 切换待办类型 | 用户可将普通节点切换为待办，或恢复为普通节点；切换不改变文本和子树 |
| FR-17 | P0 | 切换完成状态 | 待办可标记完成或未完成；完成状态有不依赖颜色的视觉标识并自动保存 |
| FR-18 | P0 | 拖拽排序 | 鼠标或触摸拖动节点后，可调整同级顺序或移动到其他父节点；整棵子树随节点移动 |
| FR-19 | P0 | 防止非法移动 | 不允许节点成为自身或任一后代的子节点；非法目标不改变数据 |
| FR-20 | P0 | 非指针替代操作 | 节点操作菜单提供上移、下移、缩进和减缩进，使排序与层级调整无需拖拽也能完成 |

### 6.5 本地保存与异常处理

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-21 | P0 | 自动保存 | 内容、结构、顺序、待办和折叠状态变化后异步写入当前 origin 的 IndexedDB，无需手动保存；结构操作立即提交，连续文字输入最迟 100ms 后提交 |
| FR-22 | P0 | 启动恢复 | 刷新页面或关闭后重新打开浏览器，恢复最近一次成功保存的数据 |
| FR-23 | P0 | 保存状态反馈 | 保存成功无需打断用户；写入失败时显示持续可见的“未保存”提示，不得显示成功状态 |
| FR-24 | P0 | 容量异常 | 捕获本地存储配额或事务失败；保留当前内存中的编辑内容，并提示本地容量不足和数据风险 |
| FR-25 | P0 | 多标签页保护 | 同一时刻只允许一个标签页写入；其他标签页进入只读，并允许用户主动接管写入权；MVP 不自动合并 |
| FR-26 | P0 | 本地数据说明 | 首次使用及设置/帮助入口明确说明数据仅在当前浏览器、清除站点数据或使用隐私模式可能导致丢失 |
| FR-27 | P0 | 导入与导出 | 以带格式版本的 JSON 无损导出和恢复整棵树；选中节点可复制/导出为 Markdown，但不承诺下划线无损往返 |

### 6.6 PWA 与离线

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-28 | P0 | PWA 基础信息 | 提供 manifest，包含名称、短名称、192px/512px 图标、`start_url` 和 standalone 显示配置 |
| FR-29 | P0 | 可安装 | 在满足平台条件的 HTTPS 环境中可按浏览器提供的方式安装；不强制自定义安装弹窗 |
| FR-30 | P0 | 离线启动 | 用户在线成功加载一次后，断网重新打开应用仍能加载应用壳和本地大纲 |
| FR-31 | P0 | 离线编辑 | 断网时新增、编辑、调整层级和完成待办均可保存到本地；恢复网络不覆盖本地数据 |
| FR-32 | P0 | 离线状态 | 网络不可用时展示不遮挡编辑的状态提示，不把“离线”误报为“保存失败” |

### 6.7 响应式布局与主题

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-33 | P0 | 响应式布局 | 桌面、平板和手机宽度下无横向页面滚动；文本编辑区、搜索和导航均可操作 |
| FR-34 | P0 | 移动端导航 | 小屏幕将次要导航收进可访问的移动菜单，聚焦路径和返回入口始终可达 |
| FR-35 | P0 | 触摸操作 | 主要点击目标适合触摸；拖拽不得阻止页面正常纵向滚动，并有节点菜单作为替代 |
| FR-36 | P0 | 明暗主题 | 支持浅色和深色；首次跟随系统，用户选择后在当前浏览器中保持 |

### 6.8 富文本、标签与批量操作

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-37 | P0 | 行内格式 | 支持粗体、斜体、删除线、行内代码和链接；刷新后格式保持（下划线降为 P1，JSON 格式层保留能力，仅 UI 不提供入口） |
| FR-38 | P0 | 节点类型 | 支持普通条目、H1/H2/H3、段落、待办、引用；切换类型不丢内容和子树（代码块和分隔线降为 P1，数据模型同构能力已预留） |
| FR-39 | P0 | 节点备注 | 每个节点可包含独立备注；按 `Shift + Enter` 聚焦备注，标题与备注均参与搜索 |
| FR-40 | P0 | Markdown 输入与粘贴 | 支持常用 Markdown 快捷输入；多行粘贴按缩进和 Markdown 结构生成节点，解析失败时保留原始文本 |
| FR-41 | P1 | 节点级多选 | 支持连续或离散选择多个节点，并批量移动、缩进、删除和复制；不要求跨节点字符选择（MVP 可通过聚焦祖先节点实现等效批量操作） |
| FR-42 | P0 | 标签与日期 | 即时识别 `#标签`、`@标签` 和 `YYYY-MM-DD` 日期（分隔符接受 `-`、`/`、`.`）；保留原始文本，可点击或搜索；点击标签或日期即将其文本填入搜索框；不解析自然语言日期，不维护标签或日期实体，搜索即普通文本匹配 |
| FR-43 | P0 | 撤销与重做 | 当前会话内统一覆盖内容与结构操作，保留最近 100 步；刷新后可清空，不构成版本历史 |
| FR-44 | P0 | 浏览器历史导航 | 聚焦节点写入当前 URL；浏览器前进、后退和刷新可恢复对应焦点 |
| FR-45 | P0 | 安全更新 | PWA 后台下载新版本，所有保存完成后提示用户重新加载；编辑中不得强制刷新 |

### 6.9 性能基线

| ID | 优先级 | 需求 | 验收标准 |
| --- | --- | --- | --- |
| FR-46 | P0 | 数据规模 | 单份大纲在 100,000 节点、50 MB 本地数据下仍可打开和编辑 |
| FR-47 | P0 | 交互延迟 | 基准设备上首屏可编辑 ≤1s；输入 P95 ≤16ms；新增、删除、移动 P95 ≤50ms；异步保存确认 P95 ≤200ms；搜索 P95 ≤100ms |
| FR-48 | P0 | 分阶段启动 | 先恢复当前焦点路径和首屏并允许编辑，其余节点与搜索索引后台加载；索引未就绪时明确显示状态 |

## 7. 无障碍与交互要求

1. 所有功能都必须可通过键盘完成，拖拽功能必须有非指针替代路径。
2. 焦点始终清晰可见；新增、删除、聚焦、关闭搜索和打开菜单后，焦点移动到可预测位置。
3. 优先使用原生输入框、按钮和链接。自定义控件必须提供正确的名称、角色、状态和键盘行为。
4. 展开/折叠、待办完成、保存失败和离线状态不能只依赖颜色表达。
5. 页面缩放至 200% 时，核心编辑、导航和搜索流程仍可使用。

## 8. 概念数据模型

每个节点至少包含：

| 字段 | 含义 |
| --- | --- |
| `id` | 当前大纲内稳定且唯一的节点标识 |
| `title` | 节点富文本标题 |
| `note` | 节点富文本备注 |
| `parentId` | 父节点标识；顶层节点指向不可见根节点 |
| `orderKey` | 当前父节点下的稳定排序位置 |
| `type` | 普通条目、标题、段落、待办、引用、代码块或分隔线 |
| `completed` | 待办完成状态 |
| `collapsed` | 子树折叠状态 |

持久化数据需包含格式版本，以便后续变更时迁移；MVP 不设计服务端实体、用户实体或协作协议。

## 9. 关键状态与边界条件

- **首次使用**：显示一个可直接输入的空节点，并说明数据保存在当前浏览器。
- **空大纲**：列表末尾渲染一个非持久化的空白输入位（UI 幻影行），数据层永远不存在空标题且无子节点的持久节点；用户在幻影行输入第一个字符时即创建真实节点，不等到失焦。
- **空搜索结果**：显示关键词和“未找到结果”，提供一键清除搜索。
- **保存失败**：保留当前页面数据与编辑能力，持续提示尚未持久化。
- **其他标签页打开**：只有一个标签页可写；其他标签页只读并提供接管入口。
- **断网**：本地编辑照常工作；只有应用资源无法加载或保存失败时才显示对应错误。
- **非法拖拽**：不移动节点，并通过目标样式或消息说明该位置不可用。

## 10. MVP 验收流程

发布前必须通过以下端到端流程：

1. 新建三个节点，将其中两个缩进成两级子节点，再减缩进；验证空节点删除，以及删除含内容节点后 toast 撤销按钮和 `Cmd+Z` 均可恢复；刷新后结构和文本一致。
2. 聚焦最深层节点，通过面包屑逐级返回根视图；数据与折叠状态不变。
3. 搜索一个跨层级关键词，在结果中编辑并完成待办；清除搜索后原树显示修改结果。
4. 拖拽包含子节点的节点到另一父节点下；刷新后节点、子树和顺序一致；尝试拖入自身后代时操作被拒绝。
5. 仅使用键盘完成新增、编辑、缩进、减缩进、折叠、待办切换、排序、搜索和聚焦。
6. 在线加载一次后断网重启应用，完成编辑并刷新；内容仍存在。
7. 模拟 IndexedDB 写入失败和第二个标签页打开；分别看到“未保存”和“只读”提示，当前编辑不被静默覆盖。
8. 在桌面、平板和手机视口完成新增、搜索、聚焦和返回；浅色、深色主题下内容和焦点均清晰可见。
9. 编辑标题与备注，应用全部行内格式和节点类型；刷新后内容无损，Markdown 粘贴可生成正确层级。
10. 导出 JSON、清空后重新导入；整棵树的内容、结构、状态和顺序一致。
11. 在 100,000 节点、50 MB 固定数据集上执行启动、输入、新增、删除、移动、保存和搜索性能验收。

## 11. 成功标准

MVP 发布需同时满足：

- 所有 `P0` 需求通过自动化测试或有记录的手工验收；
- 上述 12 条端到端流程全部通过；
- 刷新、离线重启、非法移动和存储失败测试中无静默数据丢失；
- Chrome/Edge 桌面端与 Safari iOS 的核心编辑流程可用；安装方式按平台实际能力分别记录；
- 无阻断键盘操作的焦点陷阱，主要功能均有可访问名称和非指针操作方式。

MVP 不接入行为分析或远程遥测；在没有账号与隐私方案前，以验收结果和定性可用性测试评估产品。

## 12. 风险与应对

| 风险 | 影响 | MVP 应对 |
| --- | --- | --- |
| IndexedDB 仍受配额、清理和设备性能影响 | 数据量增大时可能保存失败或迁移缓慢 | 异步增量写入、捕获事务失败、显示未保存状态，并提供无损 JSON 备份 |
| 浏览器或用户可清除本地数据 | 用户可能误以为本地数据等同备份 | 首次使用与帮助入口明确提示，并将 JSON 导入/导出纳入 P0 |
| 多标签页并发写入 | 后写入的标签页可能覆盖先前修改 | 单写入标签页锁、其他标签页只读并显式接管，不做自动合并 |
| 富文本编辑器与虚拟列表交互复杂 | IME、光标、撤销或滚动可能异常 | 仅挂载一个活动编辑器，并在 Chrome/Edge 与真实 iOS Safari 上做固定原型和回归验收 |
| PWA 安装行为因平台而异 | 用户找不到统一安装入口 | 使用标准 manifest，提供平台相关说明，不承诺统一安装弹窗 |
| 拖拽与树形编辑对键盘/触摸不友好 | 部分用户无法完成核心操作 | 提供节点操作菜单和可见焦点，按真实键盘与触摸流程验收 |

## 13. 外部依据

产品边界与验收要求参考以下一手资料，完整调研见 [`docs/research/flowlist-prd-research.md`](docs/research/flowlist-prd-research.md)：

- [Workflowy Features](https://workflowy.com/features)：无限层级、键盘新建/缩进、聚焦、面包屑和搜索的公开产品行为。
- [MDN IndexedDB API](https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API) 与 [MDN Storage quotas and eviction criteria](https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria)：异步本地数据库、存储配额和清理风险。
- [MDN Making PWAs installable](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Guides/Making_PWAs_installable) 与 [web.dev Install criteria](https://web.dev/articles/install-criteria)：PWA 安装条件及其与离线能力的区别。
- [WAI Understanding SC 2.1.1](https://www.w3.org/WAI/WCAG21/Understanding/keyboard.html) 与 [WAI-ARIA APG Keyboard Interface](https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface)：键盘可用性、焦点管理和自定义控件要求。
